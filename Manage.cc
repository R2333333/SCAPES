#include "headerFiles/Manage.h"

void Manage::create(string& filename){
  //create a file that called "filename"
  //load a working space for user to code.
}

void Manage::open(string& filename){
  //open(load) the file that called "filename"
}

void Manage::compile(string& filename){
  //read the comtent of the file compile the
  //instructions inside the file.

}

void Manage::run(string& filename){
  //run a file called "filename"
}

void Manage::changepref(){
  //change system preference.
}

void Manage::save(string& source, string& dest){
  //save a source file(program) to destination file.
  //can be the same file or save as another file.
}
